//! ACP server authentication.
//!
//! Reconstruction skeleton.

/// Authenticate an ACP connection.
///
/// TODO(reconstruction): the original
/// `chisel-agent/src/acp_server/auth.rs` validates the enterprise agent auth
/// handshake before serving ACP sessions.
pub fn authenticate(_token: Option<&str>) -> bool {
    false
}