//! ACP command prompt handling.
//!
//! Reconstruction skeleton.

/// Build the command prompt for an ACP session.
///
/// TODO(reconstruction): the original
/// `chisel-agent/src/acp_server/command_prompt.rs` composes the initial prompt
/// (system prompt + workspace context + command) for ACP sessions.
pub fn build_command_prompt(_command: &str) -> String {
    String::new()
}