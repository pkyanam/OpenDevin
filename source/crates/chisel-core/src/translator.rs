//! Translator — turns user request language into action pipelines.
//!
//! Reconstruction skeleton.

/// Translate a free-form user request into structured actions.
///
/// TODO(reconstruction): the original `chisel-core/src/translator.rs` contains
/// the prompt-driven translator that plans tool calls from a user request.
pub fn translate(_request: &str) -> Vec<String> {
    Vec::new()
}