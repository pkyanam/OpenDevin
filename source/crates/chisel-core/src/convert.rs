//! Conversion helpers (markdown/terminal/UTF-8 handling).
//!
//! Reconstruction skeleton.

/// Convert markdown text to a terminal-friendly representation.
///
/// TODO(reconstruction): the original `chisel-core/src/convert.rs` powers the
/// markdown rendering used by chisel-ui::terminal_markdown.
pub fn markdown_to_terminal(_md: &str) -> String {
    String::new()
}