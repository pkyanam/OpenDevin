//! chisel-commands — CLI subcommands for the `devin` binary.
//!
//! Reconstruction skeleton. The original crate (per reports/recon.md) also
//! contains cloud/cloud_secrets/connect/worker/checks/manual_migrations
//! subcommands which are pending reconstruction.

#![allow(dead_code)]

pub mod doctor;
pub mod models;
pub mod rules;
pub mod skills;